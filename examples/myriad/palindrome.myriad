func isPalindrome(str) {
    const length = str.length();

    let i = 0;
    while(i < length / 2) {
        if(str[i] != str[length - 1- i]) return false;

        i++;
    }

    return true;
}

const str = input('Enter a string: ');

const value = isPalindrome(str);

print(value);